# SmartBite Frontend

The uploaded SmartBite frontend archive contained the React source under `src/`, but did not contain the project-level Vite/npm metadata (`package.json`, Vite config, `index.html`, etc.).

The source has therefore been preserved without inventing dependency versions or build configuration.

Before running the frontend, restore/provide the original project-level Vite files from the development copy.
